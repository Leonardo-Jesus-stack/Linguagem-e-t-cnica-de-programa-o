#include <stdio.h>
#include <stdlib.h>

int main() {
	int base,altura;
	float area;
	
	base = 10;
	altura = 20;
	
	area = (base*altura)/2;
	
	printf("A area do triangulo Retangulo de Base = %d e altura = %d e %f", altura, area, base);
	
	return 0;
}
